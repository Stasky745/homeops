# Secrets
age.key

# Large files
*.iso

# Talos generated configs (contain secrets) and kubeconfig
talos/clusterconfig/

# Real talconfig and nodes env (contain network topology)
# Copy *.example counterparts and fill in your values
talos/talconfig.yaml
talos/nodes.env

# Cluster-wide settings (personal info, not committed)
# Copy kubernetes/cluster.env.example and fill in your values
kubernetes/cluster.env

# varlock — .env.schema is committed on purpose; resolved values never are
.env
.env.local
