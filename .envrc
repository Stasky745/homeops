source_up_if_exists

# Abort rather than leave the repo without its secret-scanning pre-commit hook.
# Unconditional: `check-install` only hashes the config, so it passes even when the hook file is gone.
watch_file lefthook.yml
lefthook install >/dev/null || exit 1
