apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: toolhive-operator-crds
  namespace: ai
spec:
  interval: 1h
  timeout: 5m
  chartRef:
    kind: OCIRepository
    name: toolhive-operator-crds
    namespace: ai
  install:
    crds: Create
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    cleanupOnFail: true
    remediation:
      retries: 3
  values: {}
