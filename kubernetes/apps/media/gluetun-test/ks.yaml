apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gluetun-test
  namespace: flux-system
spec:
  targetNamespace: media
  commonMetadata:
    labels:
      app.kubernetes.io/name: gluetun-test
  dependsOn:
    - name: app-template
    - name: external-secrets-clustersecretstore
  path: ./kubernetes/apps/media/gluetun-test/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: gluetun-test
      namespace: media
    - apiVersion: external-secrets.io/v1
      kind: ExternalSecret
      name: gluetun-test-vpn-secret
      namespace: media
