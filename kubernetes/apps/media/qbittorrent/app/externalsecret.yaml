apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: qbittorrent-vpn-secret
  namespace: media
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore
  target:
    name: qbittorrent-vpn-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: mullvad-qbittorrent-config
